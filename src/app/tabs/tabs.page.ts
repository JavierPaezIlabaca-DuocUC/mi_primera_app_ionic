import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'app-tabs', // Selector del componente, utilizado en la plantilla
  templateUrl: './tabs.page.html', // Ruta a la plantilla HTML del componente
  styleUrls: ['./tabs.page.scss'], // Ruta a los estilos CSS del componente
})
export class TabsPage implements OnInit {

  constructor() { }

  // Método que se ejecuta cuando se inicializa el componente
  ngOnInit() {
    // Lógica de inicialización (actualmente no hay lógica específica)
  }

}
